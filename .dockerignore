# 依赖与构建产物（镜像内会重新安装/构建）
node_modules
dist
tsconfig.tsbuildinfo

# 版本与 IDE
.git
.github
.idea
.vscode

# 非构建必需的本地文件
# 注意：Dockerfile 不能排除 —— BuildKit（GitHub Actions 用）对排除 Dockerfile
# 存在版本相关的兼容问题（failed to read dockerfile），且这几个文件极小
docs
data
*.md
*.http
aa.txt
