---
dev-loop: memory
format: v1
epic: advance-notice
total-merged: 1
last-merge: 2026-09-25
---

# advance-notice 交易预告单页

## 结论
- 需求：新增「交易预告单」页面；数据前端录入 + Dexie 本地存储，不涉后端接口。
- 落点（预定）：types/domain（AdvanceNotice 类型）→ db/schema 新表+版本升级 → services/advanceNoticeService → store/slices/advanceNoticeSlice → views/AdvanceNotice + 路由。
- 2026-09-25 预告单页面 + 画布 DSL 动态模板方案已提交待确认：预告单=独立表 advanceNotices（schema v16；reasonTag 六值枚举+自由文本≤200；expired 读取派生不落库；每标的单 active；P1 含 TCalculator/CostAveraging ?symbol= 预填改造）；DSL=第八区块类型 widget（扁平单层声明式 JSON，组件白名单 8 种，节点≤8/doc≤8KB，canvas_add_widget auto 动作，aiOnly 不进手动入口），画布域不开任何代码通道（含沙箱），LLM 只出 JSON 图纸前端渲染。
- 2026-09-25 widget DSL 动态模板前端端到端落地（第八区块类型，AI 自定义增量面板）：校验单一入口 utils/widgetDsl（kind stack|grid + title≤40 + nodes≤8；8 种叶子白名单 text/metric/kv/list/table/progress/tag/divider；扁平单层；doc≤8KB；tone 三值；结构违规整条拒/字符串超长裁剪/空串拒/全部载荷字段白名单 onlyKeys；table 行列数一致）；canvas_add_widget auto 级走注册表 COMMON_OPERATION_META + CANVAS_EXECUTORS.add_widget（addCanvasBlock('widget',{dsl})），改=删旧+加新；渲染 CanvasBlockContent.WidgetContent 只读 8 叶子分派（grid 两列）；aiOnly 不进手动入口；CanvasBlockFrame 徽标/canvasLayout.DATA_GUARDS.widget（复用校验器）/canvasSummary 摘要行接入；feature-map 补 widgetdsl 关键词。docs/free-canvas-template-registry.md §六 新节落档。验证：tsc 零错误，npm test 55 文件 854 用例全绿。
- 2026-09-25 画布能力提示条件携带+分层组装落地（copilot-spec D33，同日两轮修订：「每次带上」→「条件携带」→「分层组装」，保留否决服务端注册/握手的理由：片段过期漂移+首条消息两段式延迟）：注册表承载——CanvasTemplate.aiPrompt（模板专属深规格，与 operationsMeta 守卫同仓同 PR 演进，漂移=产出被静默丢弃）+ aiTriggers（includes 宽匹配触发词，宁多带 ~1KB 勿静默失败，词表按守卫拒绝日志养护）+ CANVAS_PROMPT_COMMON 公共段（通用动作/标号约定/数据纪律）；buildCanvasPromptHints(question) 组装（公共+命中段，未命中 undefined=常规对话零 token），copilotSlice canvasPromptHints(scopeId, question) 仅 canvas scope 注入（sendMessage/retryMessage 按消息内容重判零状态）。已登记五段：widget（图纸 schema 2958B，触发词「自定义面板」+快捷按钮 draft 预填「自定义面板：帮我做一张面板，展示」）/kline（换股+划线格式）/metric（值与表达式）/table（行列覆写）/text（覆写）；chart/image/file 无写操作不设。canvasWidgetPrompt.ts 已删（内容迁入注册表）。行为边界：修改面板需再次走「自定义面板」入口（与「删旧+加新」增量语义对齐）。后端职责不变「不可信输入 8KB 截断+系统提示固定区段原样拼接」，spec v1.5 D33 行与 registry §六 同步修订。**联调首轮定案权责边界（2026-09-25）：动作外壳协议（`<copilot-actions>`，后端 CopilotStatActionExtractor 私有正则解析）由后端编排系统提示宣讲（谁解析谁宣讲、全局兜底；后端需补 few-shot 示例/禁代码围栏/禁正文动作描述/最末尾闭合 + 流式转发截断 + 归档剥离）；promptHints 仅教载荷、永不包含外壳标签（测试钉死）**；前端 GlobalCopilot.MessageBubble 渲染经 stripCopilotActionBlock（copilotActions.ts 新增：完整块移除/流式半截截断/大小写容错）剥离外壳块双保险；首轮根因=模型散文输出致后端提取 actions:null（前端管线已实证无责）。验证：tsc 零错误，npm test 56 文件 866 用例全绿。

## 断点
- [断点] 下一步：联调二轮等后端落【系统动作输出规范】（外壳宣讲+流式截断+归档剥离）后重测场景 A；前端侧全部就绪（866 用例全绿）。剩余候选=①预告单页面一期实施 ②触发词表按守卫拒绝日志养护 ③外壳剥离若后端已做可评估撤前端双保险。等用户定向。
- 2026-09-24 自由画布长需求：评估+双决策确认（react-grid-layout；水平线+趋势线段）已记 memory；设计文档 docs/free-canvas-spec.md 已产出（235行，frontmatter lint 通过），status: draft 待用户评审；开发未开始。
- 2026-09-24 后端对接定案：K线维持直连；复杂指标=无状态计算即服务（前端传切片，后端零存储）；本地计算代理=单向发布模式（agent 下发 execute_local_calc 白名单动作，前端本地算完写回区块渲染，无结果上报）。两文档 draft：free-canvas-spec.md + free-canvas-backend-integration.md（lint 已过，残留旧名已清零）。
- 2026-09-24 沉淀架构修正：MQ 异步落库实际任务发布到 data 模块消费（main 只做防重/校验+投递，落库 Worker 与爬虫在 data，契约在 contract 模块）。§2.4 已更新，lint 通过。
- 2026-09-24 契约对账定案（§2.5）：fullCode=腾讯形态（sh601318）规范输入、date=YYYY-MM-DD 字符串口径、复权校验升级因子精确比对（前端 buildAdjustFactors 传 adjustFactors）；前端确认项：execute_local_calc 不走 sandboxEngine、canvasSlice 加 blockId 串行队列、cid 复用 newClientMessageId、KlineBundle 透传 adjustType、brokerService 拉能力端点。lint 通过。
- 2026-09-24 接口契约细化完成（§2.1-2.7）：compute/ask(SSE)/news/能力端点/annotations 快照沉淀（预留）均补齐前端功能+参数+返回值+错误分支；cid 复用 newClientMessageId，done 事件后前端不回传（单向发布）。lint 通过。
- 2026-09-24 后端评估 5 处修订落档：version 统一数字自增；指标数组 null 占位对齐（不截短不回传下标）；ask 删独立 actions 事件（actions 随 done 权威响应返回，与 copilot 现网一致）；能力端点补 minBars；annotations 声明非 E2EE 明文通道 + canvasId 归属校验硬门槛。lint 通过。
- 2026-09-24 公共组件复用清单落档（spec §8.2-bis）：直接复用 8 项（StockAutocomplete/ConfirmModal/copilot 底座/动作白名单/usePageContext/klineService/idGenerator/图表库）、改造复用 4 项（GlobalCopilot 内嵌化/CopilotActionCards 扩展/EmptyStateGuide 变体/沙盘KlineChart只提工具）、不可复用 3 项（QuickJS VM/serversync/沙盘KlineChart本体）、缺口新建 2 项（全局 Toast/react-grid-layout）。lint 通过。
- 2026-09-24 画布一期前端实现完成（#9-#13）：annotate_block 动作登记（domain 载荷 + copilotActions confirm 分级/守卫 asAnnotateBlockPayload + copilotActionSlice 执行器经 runBlockTask）；useCanvasContext（scopeId=canvas，标号摘要 ≤30 行，区块级快照 Click-to-Focus）；components/canvas 三件套（CanvasBlockFrame 备注区/CanvasBlockContent 七类分派/CanvasKlineChart 水平线+趋势线段+交易日吸附）；utils/canvasExpr 常量四则递归下降解析（禁 eval）；views/StockCanvas（RGL+模板面板+空画布引导+删除级联确认）；App 路由 /stock-canvas「AI 选股台」；全局 Toast（components/ui/Toast.tsx 挂 App 根，消费 app-toast 事件）。验证：tsc 零错误，npm test 819 用例全过（含 check:arch），map:features canvas 组 8 文件归组、Toast 入 app 组。
- 2026-09-24 画布对话动作套件（思维流）落地：9 个 canvas_* 动作登记（auto: add_block/refresh_klines；confirm: set_stock/update_text/set_metric/update_table/add_hline/add_trendline/remove_block + annotate_block 维持逐条确认）；copilotActions 守卫族（腾讯形态代码正则/常量四则白名单/表格 10 列 50 行/日期 YYYY-MM-DD）；「一次确认同类放行」机制（copilotApprovedCanvasTypes 内存 Set，canvas_* 前缀限定，刷新即失）；dispatchCanvasAction 统一执行器（runBlockTask 串行 + app-toast 人话摘要反馈）；确认卡加 summary 字段；useCanvasContext 摘要增强（K线划线标签/metric 表达式/对话动作提示）；空态引导改「让 AI 帮我搭画布」为主入口。验证：tsc 零错误，npm test 54 文件 833 用例全过（新增 14 守卫/路由用例）。
- 2026-09-24 v3 角色反转落地：画布 K 线唯一通道=服务器代理 GET /api/broker/klines（前端不直连/不上传/零数据管理）；直连仅存量域（沙盘/风控/做T 保留 klineService）。新建 services/brokerService.ts（Bearer 注入禁 import store；401→SessionExpiredError 现成链路；429/5xx/超时→BrokerUnavailableError；内存缓存+force 绕过+形状防御）。数据源切换：CanvasBlockContent 两处（KlineContent/ChartContent）getKline→getBrokerKlines，代理不可用→区块占位「行情服务暂不可用」+重试走代理不切直连，未登录→「登录后可查看行情」占位；canvasService.refreshAllKlines 切代理（401 上抛分流）。鉴权边界产品定案：游客不可看画布（需登录，后端零改动）。spec 同步五处落档：§一 mermaid/角色表/核心决策#3、§2.4 整节 Tombstone（管道设计迁移后端仓库 v3 §3.4/§四）、§2.5-3 Epsilon(1e-6)+定点归一化、§四降级行（代理 429/5xx/401）、§2.7/§三 残留引用清理；free-canvas-spec.md §一 数据源改代理。验证：tsc 零错误，npm test 54 文件 833 用例全过。
- 2026-09-24 broker 三端点接入完成（能力端点→compute→ask）：brokerService 增 fetchIndicatorCaps（GET /indicators，version+白名单缓存，失败保留旧缓存/首次失败隐藏选项）+ computeIndicators（POST /indicators/compute，切片≤120 根本地预校验+白名单外不发请求+POST 底座扩展）+ askBroker（POST /ask SSE，delta/done(actions 随 done)/error 事件协议复用 parseSseBlock，空闲 30s 超时+外部取消对齐 streamQuestion）。UI 接线：metric 类型增 agent 字段（indicator/label/sourceBlockId/value/unavailable）；MetricSettingsPortal 增「agent 指标」区段（能力清单×K线来源选择，minBars 本地校验，展示值取最后非 null 槽位，429/5xx→「agent 指标暂不可用」占位）；MetricContent 三态展示（数值/暖机中…/不可用）。验证：tsc 零错误，npm test 54 文件 833 用例全过。
- 2026-09-24 画布专属经纪聊天面板落地（接 POST /api/broker/ask 通道）：brokerChatSlice（纯内存态，消息/streaming/sending/abort 刷新即失）+ BrokerChatPanel（右侧抽屉：流式渲染光标、停止生成、快捷提问四条、未登录提示、CopilotActionCards 复用同一确认管线/思维流放行）；发送自动附 buildCanvasSummary 标号摘要 + 聚焦 K 线区块附 fullCode/切片（阅后即焚）；done 的 actions 交 handleCopilotActions 管线（与 copilot 通道同口径）。依赖整理：摘要纯函数下沉 utils/canvasSummary（斩断 slice→hooks→store 循环，hook 侧 re-export 保兼容）；工具条与空态引导「AI 助手/让 AI 帮我搭画布」均改开专属面板（替换 setCopilotOpen，GlobalCopilot 保留服务其他页面）。验证：tsc 零错误，npm test 54 文件 833 用例全过。
- 2026-09-24 画布 AI 入口回归 GlobalCopilot（用户定案：页面 AI 助手功能全——确认卡/后置处理管线现成，撤掉画布专属「AI 经纪」面板）：BrokerChatPanel.tsx + brokerChatSlice.ts 删除，store types/index 接线清理；StockCanvas 工具条与空态引导回退 setCopilotOpen。保留资产：askBroker 服务层（POST /api/broker/ask SSE，后续接聊天入口时复用）、canvasSummary 下沉（utils 纯函数层）。同轮模板直选修复：空态七类模板网格直选 + 工具条常驻「+ 添加」下拉（画布非空时也可手动加模板），对话与手动双入口对等。验证：tsc 零错误，npm test 54 文件 833 用例全过。
- 2026-09-24 GlobalCopilot 快捷按钮条落地（用户定案：聊天界面加引导按钮 + 可本地拦截的本地直发）：CopilotQuickAction 三模式（draft 填草稿/send 直发走 sendMessage 管线 confirm 照常出卡/local 本地拦截直执行不经 AI 零 token）+ PageContextSnapshot.quickActions 注册口（随 registry 流转，其他页面零影响）；GlobalCopilot 输入框上方常驻胶囊条（local 绿色系区分，title 提示模式语义，handleSend 支持 overrideText）；画布首批配置：七类模板+刷新行情=local（getState() 命令式纪律）、对比两只K线/加支撑位=draft、总结画布=send。验证：tsc 零错误，npm test 54 文件 833 用例全过。
- 2026-09-24 修复画布页 /api/broker 403：根因=新建 broker 通道未登记代理——vite.config.ts server.proxy 与 middleware.js（线上 Vercel 转发表+matcher）都缺 /api/broker 条目，请求未达 Spring Boot（本地后端 curl 证据：无 token 返回 401 信封「会话已失效」，端点与鉴权正常；线上域名直连 HTTP 000）。修复：两处均按 /api/kg 同模式登记（vite 走 devUpstreams.auth 跟随 DEV_UPSTREAM_ENV；middleware 走 PROXY_UPSTREAMS.online.auth，stripPrefix:false 保留前缀，matcher 补 '/api/broker/:path*'）。验证：tsc 零错误，node --check middleware.js 语法过。注意：本地 dev 需重启 vite dev server 使新代理条目生效。
- 2026-09-24 画布页移除左侧模板面板（用户反馈太占显示面积）：StockCanvas 删 aside/panelOpen 状态/工具条收起按钮/残留 LayoutTemplate+X import；添加入口收敛为工具条常驻「+ 添加」下拉（画布非空时）+ 空态七类模板网格直选，手动与对话双入口不变。验证：tsc 零错误。
- 2026-09-25 模板注册表六步迁移完成（docs/free-canvas-template-registry.md §五，文档已升 active）：① utils/canvasTemplates.ts 新建（CanvasTemplate/CanvasOperationMeta/CanvasDataCard 类型 + 七类注册对象 initData/fetchData/dataVocab/operationsMeta + COMMON_OPERATION_META 含 add_block；R2 纪律：注册表只存元数据，exec 留 slice 层）+ StockCanvas.TEMPLATES 与 canvasLayout.DEFAULT_LAYOUT 改注册表派生（单一事实源）；② addCanvasBlock defaults 字典 → getCanvasTemplate(type)?.initData?.() 收编；③ 取数收口三处：KlineContent 切 kline.fetchData、refreshAllKlines 改区块遍历经统一入口（签名 fullCodes→CanvasBlock[]）、canvasSlice 调用方传区块引用；④ canvasDataQuery.ts 拦截器（「标号 数据词」精确匹配，词表来自 dataVocab，未命中发 AI 兜底，R2：blocks 由 slice 传参）+ sendMessage 入口拦截（仅 canvas scope，卡片 role:'system' 本地行不进服务端历史、零 token，失败落 ⚠️ 卡片）+ 历史卡片尾部 5 张经 extraDetail.dataCards 计入上下文 + GlobalCopilot 卡片渲染（绿底成功/琥珀失败）；⑤ dispatchCanvasAction switch → CANVAS_EXECUTORS 执行器表 + allCanvasOperationMeta 元数据分发（逐字段等价搬运），修复 auto 级 canvas_add_block/refresh_klines 此前落 default 被静默丢弃的分发缺口，toast 加 window 防御（node 测试环境）；⑥ 索引登记 + devlog + 文档 active。验证：tsc 零错误，npm test 54 文件 837 用例全过（新增等价性 4 用例：九动作 tier/guard 对拍 + auto 直执行回归 + confirm 入队执行链路 + 未注册静默丢弃）。
