- [canvas] 「把茅台放到画布上」AI 却调 fetch_kline 且声称成功（区块数 0、actions:null）➔ D33 v1.5 触发词条件携带下放置类说法命中不了任何 aiTriggers，canvas_add_block 不进 prompt，LLM 只见语义相近的 MCP 读工具（描述以「画布」开头加重误导），拿到真数据后把「取数成功」幻觉成「落块成功」➔ 动作类能力的提示词必须无条件在场（D33 v1.6 公共段 scope 内必带），触发词只养模板专属段；MCP 工具描述避免与前端动作语义撞车 (Ref: misc)
- [copilot] 后端 409「上一次提问仍在处理中」气泡显示成「AI 服务暂不可用，请稍后重试」➔ 错误映射缺口：信封无 subCode 时走 CODE_FALLBACK 按 code 兜底，409 缺卡落入 UPSTREAM_ERROR，其固定 hint 覆盖后端 message（气泡渲染的是 hint 字段非 message）；SSE error 分支当时完全不查 CODE_FALLBACK，比信封路径更严 ➔ 后端新增错误码须同步登记 SUB_CODE_FEEDBACK/CODE_FALLBACK（或信封直接带 subCode），SSE 与信封两条路径的兜底逻辑保持一致；气泡文案走 hint 字段，改提示文案改表不改构造器 (Ref: misc)
- [canvas] AI 回复宣称「已放上画布」但只有第一块落地，真机控制台报 toast.ts too much recursion ➔ 新增 utils/toast.ts（未提交）函数体只有自调用一行；动作执行器「先建块、后 toast 汇报」，toast 抛错把 handleCopilotActions 的 for 循环整个炸掉，后续动作静默丢弃，LLM 文案又不依赖执行结果照常宣称成功 ➔ 基础设施叶子函数落盘必须真机冒烟（node 单测被 window 守卫短路测不到）；动作循环内副作用调用（toast/持久化）要么保证实现正确要么隔离异常，不能让汇报性调用打断业务动作 (Ref: misc)
