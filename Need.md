请帮我重构/编写当前项目代码。这是一个基于纯前端/PWA（数据全部存储在本地 LocalStorage/IndexedDB）的“股票交易辅助计算与做T账本工具箱”，请严格按照以下架构要求、模块拆分与计算逻辑进行开发：

---

### 一、 整体架构与导航要求
1. **PWA 与 本地数据存储**：
   - 纯客户端执行，无后端依赖。所有配置与账本均通过 `localStorage` 或 `IndexedDB` 本地持久化。
   - **数据导出与备份**：在设置/费率页面提供 JSON 备份恢复，以及 CSV 表格导出（方便 Excel 复盘）。
2. **菜单导航**：
   - 使用 **汉堡菜单 (Hamburger Menu)**，响应式适配移动端 PWA。
   - 包含 5 个主页面：`首页`、`涨跌幅计算`、`做T计算器与账本`、`成本摊薄`、`费率配置`。
   - **默认渲染页面**：`首页`。
3. **模块拆分与解耦**：
   - 独立拆分组件文件（`Home`、`ChangeRate`、`TCalculator`、`CostAveraging`、`FeeConfig`）。
   - 各计算器保持独立，做T账本仅记录交易流水，不自动联动篡改持仓成本，确保账本精准。
   - 核心计算逻辑抽离至 `mathUtils.ts`。
   - 全局 Store/Context 统一管理 **费率配置** 和 **做T账本数据**。

---

### 二、 详细功能模块规范

#### 1. 首页 (`Home`)
- 展示 PWA 工具箱概览及快捷卡片入口。
- 看板展示：累计做T笔数、累计交易摩擦成本总额、累计做T实现总净利润。

#### 2. 涨跌幅计算 (`ChangeRate`)
- 维持现有逻辑与 UI 状态不变，独立抽离组件。

#### 3. 做T计算器与按标的统计账本 (`TCalculator`)
- **A. 做T模式与计算逻辑**：
  - 支持 **正T（先买后卖/加仓T）** 与 **倒T（先卖后买/减仓T）** 切换：
    - **正T展示**：本次做T需额外占用的资金（买入总成本）。推算**保本卖出价**。
    - **倒T展示**：本次做T盘中腾出/释放的资金（卖出净回款）。反向推算**低位接回保本买入价**（即尾盘最高以什么价格买回可保证不亏损）。
  - 输入项：标的名称/代码（必填）、买/卖价格、买/卖数量、**交易备注/心得（选填）**。
  - 自动读取全局“费率配置”与免五规则。
  - 计算 **摩擦成本（规费总和）** = 买卖双向手续费 + 过户费 + 卖出印花税。
  - **盈亏判断**：
    - 未平仓（仅买/仅卖）：状态显示 `未平仓`，盈亏显示 `--`，仅计算摩擦成本。
    - 已平仓（有买有卖）：计算 `做T净利润` 与 `收益率%`。
  - 点击 **【保存至本地账本】** 将记录存入本地。

- **B. 历史记录与按标的（Ticker）汇总**（两个 Tab 切换）：
  - **Tab 1：按标的统计 (By Ticker Summary)**：
    - 按标的分组，卡片式展示：标的名称/代码、累计总买入投入、累计总卖出回收、累计摩擦成本、累计实现总净收益、做T胜率/笔数。点击展开该标的历史明细。
  - **Tab 2：所有历史明细 (All Transactions)**：
    - 列表展示所有单笔交易明细（时间、标的、模式[正T/倒T]、买卖单价数量、摩擦成本、平仓状态、净盈亏、备注）。支持单条删除与一键清空（带二次确认）。

#### 4. 成本摊薄计算器 (`CostAveraging`)
- **Tab 1：持仓变动与多批次建仓**：
  - 支持单次加减仓计算，同时支持 **动态增加多笔买入记录**，一键计算多批次混合买入后的最终平均成本与总股数。
- **Tab 2：目标成本推算（解套/补仓计算）**：
  - 输入当前成本/持仓、计划补仓单价、目标成本价。
  - 输出需要追加买入的股数及所需资金。**自动提供 A 股 100 股（整手）规范提示**（如：“建议整手下单 300 股或 400 股”）。

#### 5. 费率配置、备份与实时测算 (`FeeConfig`)
- **配置区**：
  - 佣金/手续费率（如 0.025% 即万2.5）、是否免五 Switch 开关、过户费率（0.001%）、印花税率（0.05%）。
  - 快捷预设按钮：【A股标准模板】、【港股/美股免佣模板】。
- **数据备份与导出**：
  - **JSON 导出/导入**：全量数据备份与恢复。
  - **CSV 表格导出**：一键导出做T账本为 `.csv` 文件，方便在 Excel 中打开做数据分析。
- **实时测算表格**（以 10元/股，100股，1000元总价 实时渲染）：
  - **Tab 1：买入测算表**（股票对价 1000 元、佣金、过户费、买入最终总成本）
  - **Tab 2：卖出测算表**（股票对价 1000 元、佣金、过户费、印花税、卖出净到账及交易总扣费）

---

### 三、 技术规范与计算精度

1. **数值计算防精度丢失**：
   - 必须处理 JavaScript 浮点数精度问题（建议使用 `decimal.js` 或中间步骤按金融舍入规则处理）。
   - **中国股市计费规则**：买卖各单项费用（佣金、过户费、印花税）先各自**四舍五入保留两位小数（分）**后再汇总。
2. **PWA 与离线安全**：
   - 适配 PWA Manifest 与 Service Worker 离线 Cache。
   - 所有清空与覆盖操作提供二次弹窗确认，防止误触。


以为为第一版本之后发现的问题
---

### 一、 整体架构与导航要求
1. **菜单导航（汉堡菜单 Hamburger Menu）**：
   - 响应式适配移动端与桌面端 PWA。
   - 共 6 个主页面：`首页`、`涨跌幅计算`、`做T计算器`、`成本摊薄`、`费率配置`、`数据统计`。
   - **默认渲染页面**：`首页`。
2. **模块拆分与数据解耦（关键：多批次建仓与目标推算彻底独立）**：
   - 组件拆分：`Home`、`ChangeRate`、`TCalculator`、`CostAveraging`、`FeeConfig`、`Statistics`。
   - 核心计算库：`mathUtils.ts`（高精度计算、连续涨跌幅按天计算、整手取整计算等）。
   - 全局 Store/Context 统一管理 **费率配置**、**做T历史账本** 和 **多批次建仓实盘账本**。
   - **【目标成本推算】作为纯演算工具存在，不与【多批次建仓】进行任何自动数据联动**，确保账本数据干净精准。
   - **数据安全**：全量数据保存在本地 LocalStorage/IndexedDB。所有删除、覆盖操作必须提供二次 Modal 确认弹窗。

---

### 二、 详细功能模块规范

#### 1. 首页 (`Home`)
- 展示工具箱概览卡片及各个计算器的快速跳转入口。
- 展示本地账本核心指标汇总：累计做T笔数、累计交易摩擦成本总额、累计做T实现总净利润。

#### 2. 涨跌幅计算 (`ChangeRate`)
- 增加 **【连续涨/跌停天数】** 输入（默认 1 天）。
- **按天输出计算列表**：按天数递进，详细列出：第 1 天、第 2 天 ... 第 N 天的涨停价（+10%）和跌停价（-10%），以及累计涨跌幅%。

#### 3. 费率配置与测算 (`FeeConfig`)
- **配置输入项**：
  - 手续费/佣金率（如 0.025% 即万2.5）
  - **是否免五（Switch 开关）**：
    - **未勾选（不免五）**：强制单笔最低佣金为 5 元，隐藏最低佣金输入框。
    - **已勾选（免五）**：**动态显示【最低佣金】输入框（默认 0.5 元）**。
  - 过户费率（0.001%）、印花税率（0.05%）。
- **计算规则（买卖双向独立校验最低佣金）**：
  - 买入佣金 = Math.max(买入金额 × 佣金率, 最低佣金)
  - 卖出佣金 = Math.max(卖出金额 × 佣金率, 最低佣金)
- **全局联动**：费率配置（含免五及最低佣金）全局生效，与做T计算器及成本摊薄实时联动。
- **数据备份与恢复**：支持全量配置和账本数据的 JSON 导出/导入，以及 CSV 表格导出。

#### 4. 做T计算器 (`TCalculator`)
- **模式与 UI 同步**：
  - **彻底修复 Bug**：定义并严格引用 `mode` 变量，解决 `Uncaught ReferenceError: mode is not defined` 报错，确保点击正T/倒T按钮时，按钮高亮选中状态与计算逻辑同步切换。
  - **正T（先买后卖）**：计算本次需额外占用的资金，推算**保本卖出价**。
  - **倒T（先卖后买）**：计算本次盘中腾出/释放的资金，反向推算**低位接回保本买入价**（即尾盘最高以什么价格买回可保证不亏损）。
  - 自动读取全局费率配置（买卖双向独立校验最低佣金）。
- **会话隔离**：
  - **本页面仅展示“当前打开/本次操作”产生的做T记录**。
  - 点击【保存记录】后写入本地持久化账本，本页面不堆积以往历史（以往记录统一由【数据统计】页面展示）。

#### 5. 成本摊薄计算器 (`CostAveraging`)
包含两个**完全独立、互不联动**的功能 Tab：

- **Tab 1：多批次建仓实盘账本 (Multi-batch Position Management)**：
  - **按标的多批次独立管理**：
    - 界面按 **股票标的（代码/名称）** 独立展示与操作。
    - **输入约束**：当前持股数量必须为 **100 的整数倍**。
    - **首次建仓**：输入标的名称、初始成本价与持仓数量（100整数倍），点击保存作为“第一次建仓记录”。
    - **后续新增交易（买/卖）**：
      - 输入本次交易单价与股数。
      - 减仓按照“总资金抽回”逻辑计算新成本：
        $$\text{新成本} = \frac{(\text{旧成本} \times \text{旧股数}) - \text{本次卖出净回收资金(扣规费)}}{\text{剩余股数}}$$
      - 点击【确定】后（带确认提示），计算结果更新为该标的的“当前持仓”，并将该笔交易追加至该标的的历史批次记录中。
    - **完结建仓**：提供【完成建仓/结案】按钮，点击后将该标的状态标记为“已结案”。

- **Tab 2：目标成本推算 (Target Cost Simulation - 纯演算工具)**：
  - **独立推算，不写入/改动建仓账本**。
  - **输入项**：当前持仓成本价、当前持股数量（**必须为 100 整数倍**）、计划补仓单价、**目标成本价**。
  - **推算与整手对比逻辑**：
    - 根据目标成本，反向推算理论需要补仓的股数 X。
    - 若计算出的 X 不是 100 的整数倍（例如推算需要补仓 150 股）：
      1. 展示按理论 150 股计算出的精准达成结果与所需资金；
      2. **额外展示两组实盘下单参考**：
         - **向下整手（100 股）**：展示实际补仓 100 股后的**真实达成成本**与所需资金；
         - **向上整手（200 股）**：展示实际补仓 200 股后的**真实达成成本**与所需资金。
      3. 提供【填入向下整手】和【填入向上整手】快捷填入按钮，方便用户快速进行模拟盘算。

#### 6. 数据统计 (`Statistics`)
集中收纳、分类展示与复盘所有历史做T记录和多批次买卖建仓记录：

- **顶部核心指标看板**：累计做T总净利润（红/绿高亮）、累计摩擦成本总额、做T整体胜率%与胜/负/平笔数、进行中建仓标的数 vs 已结案标的数。
- **时间范围筛选栏**：支持按【全部】、【近7天】、【近30天】动态筛选。
- **Tab 1：做T账本统计**：按标的卡片归集，展示累计投入/回收、摩擦成本、摩擦成本占毛利比%、做T胜率%、平均收益、单笔极值。点击展开显示单笔做T明细。
- **Tab 2：建仓/成本摊薄统计**：按【进行中标的】和【已完结标的】分类，时间轴展示标的从第一次建仓到历次加/减/补仓的完整实盘履历演变。

---

### 三、 技术规范与已知 Bug 修复

1. **彻底修复 `TCalculator.js` 模式切换报错**：
   - 彻底解决 `Uncaught ReferenceError: mode is not defined at updateModeUI`，确保事件监听与状态函数中 `mode` 作用域正确，实现正T/倒T按钮高亮与逻辑同步切换。
2. **校验与精度**：
   - 持仓数量严格校验为 **100 的整数倍**。
   - 数值运算使用高精度处理，单项规费保留 2 位小数，保本价与成本价保留 3 位小数。  


---

# 🤖 Cline Prompt / Project Context Document: PWA Stock Trading Calculator & T-Ledger Toolkit

> **给 Cline 的执行说明**：
> 请仔细阅读本 Markdown 文档。本项目为一个**纯前端 PWA 股票交易辅助计算与做 T 账本工具箱**。请严格按照本文档定义的**技术栈架构、模块拆分、高精度计算规则与任务拆解**逐步完成项目重构/编写。

---

## ⚙️ 1. 技术栈与架构要求

* **框架与语言**：Vite + React (TypeScript) 或 Vue 3 (TypeScript) + Tailwind CSS
* **状态管理与持久化**：`Zustand` / `Pinia` + `IndexedDB` (推荐使用 `Dexie.js`) / `LocalStorage`
* **数值高精度计算**：必须引入 `decimal.js` 或 `bignumber.js`，严禁直接使用 JavaScript 原生浮点数直接加减乘除。
* **PWA 支持**：`vite-plugin-pwa`（支持离线 Cache 与 Web App 清单）
* **图标库**：`Lucide-React` / `Lucide-Vue`

---

## 📐 2. 核心计算规则与精度规范 (`src/utils/mathUtils.ts`)

在实现任何组件之前，必须优先在 `mathUtils.ts` 中实现以下核心计算函数：

1. **规费计算与四舍五入规则（中国股市标准）**：
* 单项规费（佣金、过户费、印花税）在计算时，**必须各自先四舍五入保留 2 位小数（分）**，然后再相加汇总。
* **佣金计算**：`Math.max(金额 * 佣金率, 最低佣金)`。若未勾选“免五”，最低佣金强制设为 5 元；若勾选“免五”，按自定义最低佣金（默认 0.5 元）。
* **过户费**：`金额 * 0.001%`（买卖双向）。
* **印花税**：`金额 * 0.05%`（仅卖出单向）。


2. **精度展示**：
* 资金总额、摩擦成本、净利润：保留 **2 位小数**。
* 保本价、单位成本价：保留 **3 位小数**。


3. **整手校验与计算**：
* 持仓数量必须校验为 **100 的整数倍**。
* 目标成本推算时，提供 `floor(X / 100) * 100`（向下整手）和 `ceil(X / 100) * 100`（向上整手）的推算逻辑。



---

## 🧱 3. 模块拆分与组件路由规划

应用包含 **6 个核心页面**（通过 App Layout + Hamburger Menu 导航），默认渲染 `Home`：

```text
src/
├── components/
│   ├── layout/            # 响应式 Layout、汉堡菜单、顶部/侧边导航
│   └── ui/                # Modal 弹窗、Confirm 确认框、Card、Tabs
├── store/                 # 全局持久化状态 (FeeConfig, TLedger, PositionLedger)
├── utils/
│   ├── mathUtils.ts       # 核心金融高精度计算库
│   └── storageUtils.ts    # JSON 备份/恢复、CSV 导出
└── views/
    ├── Home.tsx           # 1. 首页：看板与快捷卡片
    ├── ChangeRate.tsx     # 2. 涨跌幅计算器（含连续涨跌停阶梯）
    ├── TCalculator.tsx    # 3. 做T计算器（独立会话，正T/倒T）
    ├── CostAveraging.tsx  # 4. 成本摊薄计算器（多批次账本 vs 目标推算）
    ├── Statistics.tsx     # 5. 数据统计与复盘（历史明细与建仓履历）
    └── FeeConfig.tsx      # 6. 费率配置与实时测算

```

---

## 📋 4. 详细模块开发规范

### 模块 1：首页 (`Home`)

* 展示概览卡片：**累计做T笔数**、**累计交易摩擦成本总额**、**累计做T实现总净利润**（正数高亮显示）。
* 快捷入口卡片导航至各计算器。

### 模块 2：涨跌幅计算器 (`ChangeRate`)

* 支持输入基础价格，计算对应涨跌幅；支持输入买卖单价，计算涨跌百分比。
* 输入 **【连续涨/跌停天数 N】**（默认 1 天），以列表展示第 1 天至第 N 天的涨停价 (+10%)、跌停价 (-10%) 以及累计涨跌幅 %。

### 模块 3：做T计算器 (`TCalculator`)

* **模式切换 Bug 防范**：严格定义并响应式绑定 `mode` 变量（`'BUY_FIRST'` | `'SELL_FIRST'`），确保 UI 按钮高亮状态与计算逻辑完全同步，彻底杜绝 `ReferenceError: mode is not defined`。
* **正T（先买后卖）**：输入买入价、买入股数、推算卖出保本价与资金占用。
* **倒T（先卖后买）**：输入卖出价、卖出股数、反向推算低位接回保本买入价与盘中腾出资金。
* **会话隔离**：本页面仅计算当前做T操作，点击【保存记录】后写入持久化 Store 并重置当前表单，本页面**不堆积**以往历史记录。

### 模块 4：成本摊薄计算器 (`CostAveraging`)

包含两个**完全独立、互不联动**的 Tab：

* **Tab 1：多批次建仓实盘账本**：按标的分组管理。支持首次建仓、加仓、减仓（采用**总资金抽回法**计算新成本）。输入框严格校验 100 整数倍。提供【完结建仓/结案】操作。
* **Tab 2：目标成本推算 (纯演算工具)**：输入当前成本、当前股数、计划补仓单价、目标成本价。反向推算所需股数 $X$。若 $X$ 非整手，同步输出“向下整手”与“向上整手”实盘数据，并提供快捷填充按钮。

### 模块 5：数据统计与复盘 (`Statistics`)

* 全局时间筛选器：【全部】、【近7天】、【近30天】。
* **Tab 1：做T账本统计**：按标的汇总展示胜率、摩擦成本占比、累计净利润；可展开查看单笔明细，支持单条删除与清空（需 Modal 确认）。
* **Tab 2：建仓履历统计**：按【进行中】/【已结案】分类，时间轴展示多批次加减仓过程。

### 模块 6：费率配置与数据管理 (`FeeConfig`)

* 佣金率、免五 Switch（免五开启时可自定义最低佣金，默认 0.5 元；不免五强制 5 元）、过户费率、印花税率。
* 提供预设按钮：【A股标准模板】、【港股/美股免佣模板】。
* 提供 JSON 导入/导出全量数据，以及 CSV 表格导出做T账本。
* 实时测算表格：以 $10$ 元/股、$100$ 股为基准，实时渲染买入/卖出费用拆解表。

---

## 🛠️ 5. 给 Cline 的分步执行计划 (Execution Steps)

请 Cline 按照以下顺序逐一完成开发：

* [ ] **Task 1: 项目初始化与基础基础设施构建**
* 配置 Vite + Tailwind + Decimal.js + Zustand/Dexie + PWA 插件。
* 实现 `src/utils/mathUtils.ts` 中的四舍五入与规费逻辑。
* 建立响应式 Layout 与汉堡菜单导航（6 个 Page 路由/视图切换）。


* [ ] **Task 2: 实现全局 Store 与费率配置页面 (`FeeConfig`)**
* 构建全局状态与持久化逻辑。
* 实现 `FeeConfig` 组件（模板切换、免五 Switch 动态显示、实时测算表格、JSON/CSV 导入导出）。


* [ ] **Task 3: 实现首页 (`Home`) 与涨跌幅计算器 (`ChangeRate`)**
* 编写 `Home` 概览看板。
* 编写 `ChangeRate`，实现连续 $N$ 天涨跌停阶梯推算。


* [ ] **Task 4: 实现做T计算器 (`TCalculator`)**
* 实现正T/倒T模式切换（确保作用域安全与状态同步）。
* 接入全局费率推算保本价与摩擦成本。
* 实现做T记录持久化保存与会话隔离重置。


* [ ] **Task 5: 实现成本摊薄计算器 (`CostAveraging`)**
* 实现 Tab 1：多批次建仓实盘账本（100 股校验、总资金抽回法减仓成本计算、结案归档）。
* 实现 Tab 2：目标成本推算（整手向下/向上对比推算与快捷填充，保证完全独立不联动 Tab 1）。


* [ ] **Task 6: 实现数据统计页面 (`Statistics`)**
* 实现时间筛选、做T卡片归集与明细展开。
* 实现建仓履历时间轴展示。
* 为所有删除/清空/覆盖操作增加二次弹窗确认 (Confirm Modal)。 